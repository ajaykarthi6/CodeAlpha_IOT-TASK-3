
IoT Sensor-LED System — Complete Wiring Diagram
================================================
Platform  : Arduino UNO (ATmega328P, 5V, 16 MHz)
Sensors   : TMP36 | LDR + 10kΩ | PIR HC-SR501
Actuators : Red LED (D9) | Blue LED (D10) | Yellow LED (D11)


                         ┌───────────────────────────────┐
                         │         ARDUINO UNO           │
                         │                               │
    TMP36                │                               │
    ┌─────┐              │  A0 ◄──── VOUT                │
    │ VCC ├───── 5V ────►│  5V                           │
    │ GND ├───── GND ───►│  GND                          │
    │VOUT ├──────────────►  A0   (Analog Input)          │
    └─────┘              │                               │
    Vout = (T°C+50)×10mV │                               │
                         │                               │
    LDR Voltage Divider  │                               │
    ┌───┐                │  A1 ◄──── mid-point           │
    │LDR├── 5V           │                               │
    │   ├──┬─────────────►  A1   (Analog Input)          │
    └───┘  │             │                               │
           │ 10kΩ        │                               │
           └──── GND     │                               │
    (dark→low ADC)       │                               │
                         │                               │
    PIR HC-SR501         │                               │
    ┌──────────┐         │  D2 ◄──── OUT                 │
    │  VCC ────├── 5V ──►│  5V                           │
    │  GND ────├── GND ─►│  GND                          │
    │  OUT ────├─────────►  D2    (Digital Input)        │
    └──────────┘         │  HIGH = motion detected       │
                         │                               │
                         │                               │
                         │  D9  ──► [220Ω] ──► [RED LED]  ──► GND
                         │         Temperature alert     │
                         │                               │
                         │  D10 ──► [220Ω] ──► [BLUE LED] ──► GND
                         │         Night / dark alert    │
                         │                               │
                         │  D11 ──► [220Ω] ──► [YEL LED]  ──► GND
                         │         Motion alert          │
                         │                               │
                         └───────────────────────────────┘


Pin Summary Table
─────────────────────────────────────────────────────────────────
 Component        Pin   Type         Series R   Function
─────────────────────────────────────────────────────────────────
 TMP36 Sensor     A0    Analog IN    —          Temp 0–5 V out
 LDR + 10kΩ       A1    Analog IN    10 kΩ      Light ADC 0–1023
 PIR HC-SR501     D2    Digital IN   —          Motion HIGH/LOW
 Red LED          D9    Digital OUT  220 Ω      Heat alert
 Blue LED         D10   Digital OUT  220 Ω      Dark / night
 Yellow LED       D11   Digital OUT  220 Ω      Motion alert
─────────────────────────────────────────────────────────────────


TMP36 Voltage-to-Temperature Formula
─────────────────────────────────────
  ADC_raw  = analogRead(A0)            // 0 – 1023
  Voltage  = ADC_raw × (5.0 / 1023.0) // 0 – 5 V
  Temp_°C  = (Voltage − 0.5) × 100    // °C

  Example: ADC=563 → 2.75 V → 22.5°C


LDR Voltage Divider Behaviour
──────────────────────────────
  Bright light → LDR resistance drops → higher voltage at A1 → high ADC
  Darkness     → LDR resistance rises → lower voltage at A1 → low ADC
  Threshold: ADC < 300 → BLUE LED ON (night mode)


LED Current-Limiting Resistor Calculation
──────────────────────────────────────────
  Arduino output = 5 V
  LED forward voltage (Vf) ≈ 2.0 V
  Desired current (If)     = 15 mA

  R = (Vcc − Vf) / If = (5.0 − 2.0) / 0.015 = 200 Ω → use 220 Ω (nearest standard)
